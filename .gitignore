/target
/corpus
/tests/fixtures/*.epub
__pycache__/

# wasm-pack build output (generated npm package) — not committed.
/epubveri-wasm/pkg/

# macOS Finder metadata
.DS_Store

# Local-only project context / history — not for publication (owner decision).
/CLAUDE.md
/docs/PROGRESS_LOG.md
/docs/UPSTREAM_TRIAGE.md

# Claude Code per-machine settings. Local, not project content — and iCloud
# makes conflict copies of them ("settings.local 2.json"), the same class of
# stray file that once shipped inside the 0.7.2 npm tarball.
/.claude/settings.local*.json

# Drafts and one-off measurements that outlive a session but are not the work:
# the session scratchpad lives under /private/tmp and is gone on restart, and
# `~/Documents` is not a place to litter (owner, 2026-09-04). CLAUDE.md already
# refers to files here, e.g. the 981-book compare baseline.
/scratchpad/
